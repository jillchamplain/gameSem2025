using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum ManagerType
{
    NULL = -1,
    UI = 0,
    COW = 1,
    FOOD = 2,
    TRAIN = 3,
    RACE = 4,
    PARTICLE = 5,
}
